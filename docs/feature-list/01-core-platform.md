# 01. Core Platform & Orchestration

## 1.1 Root Runtime & Foundation
- `[🤖 AI Enhancement (Cycle 1, Task 1.1.01)]` Minimalist Node.js / TypeScript root project configuration, dependency wiring, and zero-build single-command launcher (`./run.sh` / `npm start`).
- `[👤 User Direction]` Support loading environment configuration from `.env` (e.g. `PORT=4004`, `HOST=0.0.0.0`) automatically on platform startup.

## 1.2 HUD Static Delivery Plugin
- `[🤖 AI Enhancement (Cycle 1, Task 1.2.01)]` Pluggable Cordis static delivery service (`HudStaticService`) that streams the SAM3 HUD visual workspace over HTTP without modifying submodule contents.

## 1.3 Core Lifecycle & Status Heartbeat
- `[🤖 AI Enhancement (Cycle 1, Task 1.3.01)]` Cordis microkernel application bootstrap with reactive lifecycle hooks, status heartbeat logging, and graceful signal shutdown handling.
